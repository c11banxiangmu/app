<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="cn.dao.CateGoryMapper">
 
	
	<select id="getCategory" resultType="app_category" parameterType="int">
		SELECT
		id,categoryName,parentId FROM app_category
		WHERE parentId=#{parentId}
	</select>
	
	

 	<select id="getpingtaiByName" resultType="data_dictionary">
 		SELECT valueName FROM data_dictionary 
		WHERE typeName='所属平台'
 	
 	</select>
 </mapper>