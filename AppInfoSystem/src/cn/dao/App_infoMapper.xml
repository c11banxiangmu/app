<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.app_infoMapper">
<!-- 查询所有app信息 -->
	<select id="getAppInfo" resultType="app_info">
		SELECT softwareName,APKName,softwareSize,flatformId,categoryLevel1,categoryLevel2,categoryLevel3,status,downloads,versionId FROM app_info
		<where>
			<if test="softwareName!=null and softwareName!=''">
				and softwareName like concat('%',#{softwareName},'%')
			</if>
			<if test="status!=0">
				and status=#{status}
			</if>
			<if test="flatformId!=0">
				and flatformId=#{flatformId}
			</if>

		</where>
	</select>
	
</mapper>